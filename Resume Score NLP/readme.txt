change the file name to resume_scorer.py

run it using uvicorn 
 command to run this file is::uvicorn resume_scorer:app --reload

install all the dependencies in anaconda python 

and run the file by going into the link and adding /docs after it 


example::http://127.0.0.1:8000/docs

